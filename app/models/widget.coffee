# mongoose = require "mongoose"

# Widget = new mongoose.Schema
#   name: String
#   desc: String
#   created_at: { type: Date, default: Date.now }

# # Not sure if if this necessary
# module.exports = mongoose.model "Widget", Widget
